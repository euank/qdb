webdata:
  build: ./frontend

web:
  build: ./nginx/
  volumes_from:
    - webdata
  ports:
    - "8093:80"
  links:
    - api

db:
  build: db/

migrations:
  build: db_migrations/
  links:
    - db

api:
  build: quick_backend/
  links:
    - db
